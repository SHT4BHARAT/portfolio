# Google Apps Script Web App URL
# Create a Google Sheet → Extensions → Apps Script → Paste the script from docs/gas-backend.gs
# Deploy → New deployment → Web app → Copy URL
PUBLIC_GAS_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
